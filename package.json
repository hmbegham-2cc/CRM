{
  "name": "crc-platform",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.18.1",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
